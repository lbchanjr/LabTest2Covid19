<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.louisechan.labtest2covid19">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditCaseActivity" android:noHistory="true"></activity>
        <activity android:name=".ViewCasesActivity" />
        <activity android:name=".AddCaseActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RegularUserActivity" />
        <activity android:name=".ClerkActivity" />
        <activity android:name=".MainActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>